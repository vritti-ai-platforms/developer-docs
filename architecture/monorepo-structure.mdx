---
title: Monorepo Structure
description: Organization of the Vritti workspace and projects
---

Vritti uses a multi-repo approach with a workspace configuration that links all projects together. This enables consistent tooling while allowing independent deployment of each service.

## Workspace Overview

```
/Users/shashankraju/Documents/Vritti/
├── vritti.code-workspace          # VS Code workspace config
│
├── quantum-ui/                    # Shared UI component library
│   ├── src/components/
│   ├── src/lib/
│   └── package.json              # @vritti/quantum-ui
│
├── api-sdk/                       # Shared NestJS modules
│   ├── src/auth/
│   ├── src/database/
│   └── package.json              # @vritti/api-sdk
│
├── api-nexus/                     # Platform backend API
│   ├── src/
│   ├── src/db/schema/
│   └── package.json
│
├── cloud-server/                  # Cloud product backend API
│   ├── src/
│   ├── src/db/schema/
│   └── package.json
│
├── web-nexus/                     # Frontend host app (Module Federation)
│   ├── src/
│   └── package.json
│
├── cloud-microfrontend/           # Cloud dashboard micro-frontend
│   ├── src/
│   └── package.json
│
├── cloud-web/                     # Standalone auth + account web app
│   ├── src/
│   └── package.json
│
├── landing/                       # Next.js landing page
│   ├── app/
│   └── package.json
│
├── infrastructure/                # Terraform + Docker infrastructure
│   ├── terraform/
│   └── docker/
│
├── slicingpie/                    # Admin dashboard
│   ├── src/
│   └── package.json
│
└── developer-docs/                # This documentation
    ├── docs.json
    └── *.mdx
```

## Project Categories

### Shared Libraries

These packages are published to npm and consumed by other projects:

<Tabs>
  <Tab title="@vritti/quantum-ui">
    **Purpose:** Shared React component library based on shadcn/ui

    ```bash
    # Location
    /quantum-ui

    # Package name
    @vritti/quantum-ui@0.2.x

    # Key exports
    - UI Components (Button, Card, Dialog, etc.)
    - Form components (with react-hook-form integration)
    - Axios instance with CSRF and auth interceptors
    - Toast notifications
    ```

    **Consumers:**
    - web-nexus
    - cloud-web
    - cloud-microfrontend
    - slicingpie
  </Tab>
  <Tab title="@vritti/api-sdk">
    **Purpose:** Shared NestJS modules for backend services

    ```bash
    # Location
    /api-sdk

    # Package name
    @vritti/api-sdk@0.2.x

    # Key exports
    - AuthConfigModule (JWT guards, decorators)
    - DatabaseModule (Drizzle ORM, multi-tenant)
    - LoggerModule (Winston-based logging)
    - Base repositories for common patterns
    ```

    **Consumers:**
    - api-nexus
    - cloud-server
  </Tab>
</Tabs>

### Backend Services

<CardGroup cols={2}>
  <Card title="api-nexus" icon="server">
    **Platform API Gateway**

    The primary backend handling auth, tenant management, users, and onboarding flows.

    ```
    /api-nexus
    ├── src/
    │   ├── db/
    │   │   ├── schema/         # Drizzle ORM schemas
    │   │   └── migrations/     # Database migrations
    │   ├── modules/
    │   │   └── cloud-api/      # All API modules
    │   │       ├── auth/       # Authentication
    │   │       ├── onboarding/ # Verification flows
    │   │       ├── tenant/     # Multi-tenant management
    │   │       └── user/       # User management
    │   └── services/           # Shared services
    └── drizzle.config.ts
    ```

    **Port:** 3000 (API), 3000/api/docs (Swagger)
  </Card>
  <Card title="cloud-server" icon="cloud">
    **Cloud Product API**

    Backend for the cloud dashboard — companies, deployments, AI chat, and admin APIs.

    ```
    /cloud-server
    ├── src/
    │   ├── db/
    │   │   ├── schema/         # Drizzle ORM schemas
    │   │   └── migrations/     # Database migrations
    │   ├── modules/
    │   │   ├── cloud-api/      # Cloud product routes
    │   │   ├── admin-api/      # Admin routes
    │   │   ├── companies/      # Company management
    │   │   └── ai-chat/        # AI chat features
    │   └── services/           # Shared services
    └── drizzle.config.ts
    ```

    **Port:** 3000 (API), 3000/api/docs (Swagger)
  </Card>
</CardGroup>

### Frontend Applications

<CardGroup cols={2}>
  <Card title="web-nexus" icon="window">
    **Host Application**

    Module Federation host that loads remote micro-frontends at runtime.

    ```
    /web-nexus
    ├── src/
    │   ├── config/remotes.config.ts
    │   ├── utils/RemoteRoutes.tsx
    │   └── App.tsx
    └── rsbuild.config.ts
    ```

    **Port:** 3012
  </Card>
  <Card title="cloud-microfrontend" icon="chart-bar">
    **Cloud Dashboard Remote**

    Module Federation remote serving the cloud product dashboard.

    ```
    /cloud-microfrontend
    ├── src/
    │   ├── features/
    │   ├── pages/
    │   ├── hooks/
    │   └── routes.tsx
    └── rsbuild.config.ts
    ```

    **Port:** 3002
  </Card>
  <Card title="cloud-web" icon="fingerprint">
    **Auth + Account Web App**

    Standalone auth and account management app. Also exposed as a Module Federation remote for the host.

    ```
    /cloud-web
    ├── src/
    │   ├── pages/
    │   ├── context/
    │   └── routes.tsx
    └── rsbuild.config.ts
    ```

    **Port:** 3001
  </Card>
  <Card title="landing" icon="globe">
    **Marketing Site**

    Next.js 15 application for public-facing pages.

    ```
    /landing
    ├── app/
    │   ├── page.tsx
    │   └── layout.tsx
    └── next.config.ts
    ```

    **Port:** 3003
  </Card>
  <Card title="slicingpie" icon="chart-pie">
    **Admin Dashboard**

    Internal admin interface for platform management.

    ```
    /slicingpie
    ├── src/
    │   └── ...
    └── rsbuild.config.ts
    ```

    **Port:** 3004
  </Card>
</CardGroup>

### Infrastructure

<Card title="infrastructure" icon="server">
  **Infrastructure as Code**

  Terraform and Docker configuration for all environments.

  ```
  /infrastructure
  ├── terraform/         # Cloud infrastructure definitions
  └── docker/            # Docker Compose files for local dev
  ```
</Card>

### Documentation

<Card title="developer-docs" icon="book">
  **This Documentation**

  Mintlify-powered developer documentation with auto-generated API reference from OpenAPI.

  ```
  /developer-docs
  ├── docs.json              # Mintlify config
  ├── openapi.json           # Auto-generated API spec
  ├── getting-started/       # Onboarding guides
  ├── architecture/          # System architecture
  ├── projects/              # Per-project docs
  ├── features/              # Feature documentation
  ├── api/                   # API reference pages
  ├── guidelines/            # Development standards
  └── operations/            # Deployment & ops
  ```
</Card>

## VS Code Workspace

The `vritti.code-workspace` file configures VS Code for multi-project development:

```json
{
  "folders": [
    { "path": "quantum-ui", "name": "quantum-ui" },
    { "path": "api-sdk", "name": "api-sdk" },
    { "path": "api-nexus", "name": "api-nexus" },
    { "path": "cloud-server", "name": "cloud-server" },
    { "path": "web-nexus", "name": "web-nexus" },
    { "path": "cloud-microfrontend", "name": "cloud-microfrontend" },
    { "path": "cloud-web", "name": "cloud-web" },
    { "path": "landing", "name": "landing" },
    { "path": "infrastructure", "name": "infrastructure" },
    { "path": "slicingpie", "name": "slicingpie" },
    { "path": "developer-docs", "name": "docs" }
  ],
  "settings": {
    "typescript.tsdk": "node_modules/typescript/lib"
  }
}
```

<Tip>
  Open the workspace with: `code vritti.code-workspace`
</Tip>

## Package Management

All projects use **pnpm** for dependency management:

```bash
# Install dependencies in a project
cd api-nexus
pnpm install

# Link local packages during development
# In quantum-ui:
pnpm build && pnpm link --global

# In consuming project:
pnpm link --global @vritti/quantum-ui
```

## Build Configuration

Each project uses appropriate build tools:

| Project | Build Tool | Config File |
|---------|------------|-------------|
| quantum-ui | tsup | tsup.config.ts |
| api-sdk | tsup | tsup.config.ts |
| api-nexus | NestJS CLI | nest-cli.json |
| cloud-server | NestJS CLI | nest-cli.json |
| web-nexus | Rsbuild | rsbuild.config.ts |
| cloud-microfrontend | Rsbuild | rsbuild.config.ts |
| cloud-web | Rsbuild | rsbuild.config.ts |
| landing | Next.js | next.config.ts |
| slicingpie | Rsbuild | rsbuild.config.ts |

## Environment Files

Each project has its own environment configuration:

```
project/
├── .env.example         # Template with all variables
├── .env                  # Local development (git-ignored)
├── .env.staging         # Staging environment
└── .env.production      # Production environment
```

<Warning>
  Never commit `.env` files. Use `.env.example` as the template and document all variables in the [Environment Variables](/getting-started/environment-variables) guide.
</Warning>

## Next Steps

<CardGroup cols={2}>
  <Card title="Project Relationships" icon="diagram-project" href="/architecture/project-relationships">
    Understand dependencies between projects
  </Card>
  <Card title="Local Setup" icon="laptop-code" href="/getting-started/local-setup">
    Set up your development environment
  </Card>
</CardGroup>
